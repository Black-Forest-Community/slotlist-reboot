# Database Configuration
DB_DATABASE=slotlist
DB_USERNAME=postgres
DB_PASSWORD=secure_password_here

# Django Configuration
DJANGO_SECRET_KEY=change-this-to-a-random-50-character-string
CONFIG_JWT_SECRET=change-this-to-a-random-string
CONFIG_JWT_ISSUER=https://slotlist.online
CONFIG_JWT_AUDIENCE=https://slotlist.online

# Steam API Configuration (required for authentication)
CONFIG_STEAM_API_KEY=your_steam_api_key_here

# Let's Encrypt Configuration
ACME_EMAIL=admin@slotlist.online

# Traefik Dashboard Auth (optional, default: admin/admin)
# Generate with: htpasswd -nb admin your_password
# Or use: echo $(htpasswd -nb admin password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH=admin:$$apr1$$8EVjn/nj$$GiLUZqcbueTFeD23SuB6x0

# Watchtower - Automatic Updates
# Optional: Enable notifications for updates
# Supported: email, slack, msteams, gotify, pushover, etc.
# See: https://containrrr.dev/watchtower/notifications/
# WATCHTOWER_NOTIFICATIONS=slack
# WATCHTOWER_NOTIFICATION_URL=https://hooks.slack.com/services/xxx/yyy/zzz
